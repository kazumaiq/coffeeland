# ☕ Coffee Land — Premium Coffee Shop Web App

Современное мобильное приложение для кофейни с системой лояльности, админ-панелью и премиальным тёмным дизайном.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd c:/project
npm install
```

### 2. Запуск в режиме разработки

```bash
npm run dev
```

Это запустит одновременно:
- Backend сервер на http://localhost:3000
- Frontend Vite на http://localhost:5173

### 3. Открыть приложение

http://localhost:5173

### 4. Админ-панель

- URL: http://localhost:5173/admin
- Логин: `admin`
- Пароль: `admin`

## ✨ Возможности

✅ **Премиальный тёмный интерфейс** - Современный дизайн с золотыми акцентами
✅ **Система лояльности** - 10% автоматическая скидка для участников
✅ **Аутентификация** - Модальная форма входа/регистрации
✅ **Профиль пользователя** - История заказов, статус карты лояльности
✅ **Админ-панель** - Управление пользователями, выдача/отзыв карт, статистика
✅ **Красивые анимации** - Framer Motion с physics-based движением
✅ **Мобильный адаптив** - Полностью отзывчивый дизайн
✅ **Многоязычность** - Поддержка русского и английского

## 📁 Структура проекта

```
c:/project/
├── src/
│   ├── components/
│   │   ├── Hero.jsx              # Hero-секция с CTA
│   │   ├── AuthModal.jsx         # Модаль входа/регистрации
│   │   ├── LoyaltyCard.jsx       # Карта лояльности
│   │   ├── MenuItem.jsx          # Карточка товара
│   │   ├── MenuSection.jsx       # Раздел меню
│   │   ├── Header.jsx            # Шапка с аватаром
│   │   ├── Cart.jsx              # Корзина с скидкой
│   │   ├── OrderButton.jsx       # Кнопка заказа
│   │   └── LanguageSwitch.jsx    # Переключатель языка
│   ├── pages/
│   │   ├── Home.jsx              # Главная страница
│   │   ├── Account.jsx           # Профиль пользователя
│   │   ├── Admin.jsx             # Админ-панель
│   │   ├── Login.jsx
│   │   └── Register.jsx
│   ├── data/
│   │   └── menu.json             # Меню товаров
│   ├── i18n/
│   │   ├── en.json               # Переводы на английский
│   │   ├── ru.json               # Переводы на русский
│   │   └── index.js              # i18n контекст
│   ├── styles/
│   │   └── main.css              # Все стили (1000+ строк)
│   ├── App.jsx                   # Главный компонент с роутингом
│   └── main.jsx
├── server/
│   ├── index.js                  # Express сервер + SQLite
│   ├── routes/
│   │   ├── orders.js             # Логика заказов
│   │   └── users.js              # Auth + управление картами
│   └── database.db               # SQLite база данных
├── public/
│   └── logo/
├── vite.config.js
├── package.json
└── README.md
```

## 🎨 Дизайн

- **Основной цвет фона:** #1a1a1a (тёмный)
- **Акцент:** #d4a574 (кофейно-золотой)
- **Шрифт:** Inter
- **Эффекты:** Glassmorphism, мягкие тени, spring-анимации

## 🔐 Безопасность

- Скидки рассчитываются на **сервере** (защита от клиентского манипулирования)
- Admin-панель требует аутентификацию через `x-admin` заголовок
- Пароли хранятся в БД (готово к добавлению хеширования)

## 📊 API Endpoints

### Users
- `POST /api/users/login` - Вход/регистрация
- `GET /api/users/admin/users` - Список пользователей (админ)
- `PATCH /api/users/admin/users/:id/loyalty` - Выдать/отозвать карту

### Orders
- `POST /api/orders` - Создать заказ (с расчётом скидки)
- `GET /api/orders?phone=XXX` - История заказов пользователя
- `GET /api/orders` - Все заказы (админ)
- `GET /api/orders/stats` - Статистика (админ)
- `PUT /api/orders/:id/status` - Изменить статус заказа

## 💾 База данных

SQLite с таблицами:
- `users` - Пользователи
- `loyalty_cards` - Карты лояльности со статусом (active/inactive)
- `orders` - Заказы с discount_applied и final_total
- `order_items` - Товары в заказах

Скидка автоматически применяется при статусе карты `active`.

## 🛠️ Разработка

### Добавить новый товар в меню

Edit `src/data/menu.json`:

```json
{
  "id": "cappuccino",
  "name_ru": "Капучино",
  "name_en": "Cappuccino",
  "sizes": [
    {"size": "S", "price": 120},
    {"size": "L", "price": 160}
  ]
}
```

Добавить эмодзи в `MenuItem.jsx`:

```javascript
const emojis = {
  cappuccino: '🫗'
}
```

### Добавить перевод

Edit `src/i18n/ru.json` и `src/i18n/en.json` синхронно.

### Запустить Production build

```bash
npm run build
NODE_ENV=production npm start
```

## 📱 Тестирование

- **Вход:** Любой номер телефона (создаст пользователя)
- **Выдача карты:** Админ-панель → пользователи → "Give Card"
- **Скидка:** После выдачи карты товары покажут "10% Discount"

## 📞 Контакты

Проект: Coffee Land v1.0
Дата: Февраль 2026

Примечание: конфигурация сервера в `server/index.js` автоматически раздаёт `dist/` при `NODE_ENV=production`.
